# Fleeted API server — all variables optional (defaults work out of the box).
# Copy to `.env` and adjust if needed.
PORT=4000
DB_PATH=./data/fleeted.db
JWT_SECRET=haha-lmao-funny-32-digit-string
JWT_EXPIRES_IN=24h